from __future__ import annotations

# Default capacity for the Crypt storage (persistent between runs)
CRYPT_CAPACITY: int = 3

# Default save file path for the Crypt persistence layer
DEFAULT_CRYPT_SAVE_PATH: str = "saves/crypt.json"
